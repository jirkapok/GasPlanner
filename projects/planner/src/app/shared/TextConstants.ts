export class TextConstants {
    public static readonly depthConverterWarning = 'This calculator uses simple depth conversion (where 1 ATM = 1 bar), so the' +
        'values shown here are less precise, than in rest of the application.';
}
